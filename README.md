# Mpalarina NODEJS web framework 
## open source web framework

```javascript
const { main } = require("mpalarina");

;(async () => {
    await main(require("path").resolve())
    //console.log({pro})
})()
```

inspired by ruby on rails and fastapi
created by Mike Karypidis

